#version 330 core

layout (location = 0) in int block_type;
layout (location = 1) in int surface_mesh;
layout (location = 2) in vec3 world_pos;

uniform mat4 view;
uniform mat4 projection;

void main()
{
    gl_Position = vec4(world_pos, float(surface_mesh));
}
